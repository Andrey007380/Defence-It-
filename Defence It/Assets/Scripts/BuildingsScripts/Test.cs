using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
   
    void Start()
    {
        //BulldingsGrid<bool> grid = new BulldingsGrid<bool>(100, 100, 4f, new Vector3(500, 0, 500), (BulldingsGrid<bool> g, int x, int y) => new bool());
    }

   
}
